import React from 'react';

// Minimal inert SearchBar so other components that import it do not break.
export default function SearchBar(props) {
	// Search removed from hero — this component intentionally returns null.
	// Keep this file in place to avoid breaking imports. Remove entirely if not used.
	return null;
}
